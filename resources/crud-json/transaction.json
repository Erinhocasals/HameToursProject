{
    "fields": [{
            "name": "price",
            "type": "decimal"
        },
        {
            "name": "user_id",
            "type": "integer"
        },
        {
            "name": "type",
            "type": "select",
            "options": {
                "1": "in",
                "2": "out"
            }
        }
    ],
    "foreign_keys": [{
        "column": "user_id",
        "references": "id",
        "on": "user",
        "onDelete": "cascade"
    }],
    "relationships": [{
        "name": "user",
        "type": "belongsTo",
        "class": "App\\Models\\User"
    }],
    "validations": [{
        "field": "price",
        "rules": "required|max:256"
    }]
}
